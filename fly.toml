app = "rshu-ai"
primary_region = "sin"   # Singapore — terdekat dari Surabaya

[build]

[http_service]
  internal_port = 8080
  force_https   = true
  auto_stop_machines = false
  auto_start_machines = true

[[vm]]
  cpu_kind = "shared"
  cpus     = 1
  memory   = "512mb"

[env]
  PORT = "8080"
